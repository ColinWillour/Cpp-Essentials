#include <iostream>
#include <vector>
using std::endl, std::cout, std::string, std::vector;

int countWords(const string& s) {
	int words = 0;
	for (char& c : s) {
		if (std::isalpha(c) == false && std::isalpha() {
			words++;
		}
	}
}
